@model IEnumerable<ECommerce.Data.Models.promotion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/IndexLayoutSupplier.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idPromotion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateDebut)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateFin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nomPromotion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.taux)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idPromotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateDebut)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateFin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nomPromotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.taux)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.idPromotion  }) |
            @Html.ActionLink("Details", "Details", new { id = item.idPromotion }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.idPromotion })
        </td>
    </tr>
}

</table>
